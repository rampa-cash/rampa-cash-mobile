<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.RampaCashMobile" parent="android:Theme.Material.NoActionBar">
        <!-- System UI Configuration for dark theme -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar">false</item>
        
        <!-- AndroidX Splash Screen (works on all versions) - Using solid dark color -->
        <!-- Note: Splash screen background only supports solid colors, not gradients -->
        <item name="android:windowSplashScreenBackground">@color/carbon_base</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/rampa_icon_splash_foreground</item>
        <item name="android:windowSplashScreenAnimationDuration">3000</item>
        <item name="android:windowSplashScreenIconBackgroundColor">@android:color/transparent</item>
    </style>
</resources>